This is GUI JSON for the Grid Renderer system.

It is invoked like this:

```
[
  [
    [
      "__function.render_grid_from_json_data.(__get.widget_static).__string_append.output.primary"
    ]
  ]
]
```

